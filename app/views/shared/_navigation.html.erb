<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_path, class: 'navbar-brand' do %>
        <% if menu_active?(:brand) %>
          <%= fa_icon 'cutlery inverse border' %>
        <% else %>
          <%= fa_icon 'cutlery border' %>
        <% end %>
      <% end %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li class="<%= menu_active?(:recipes)? 'active' : nil %>">
          <%= link_to 'Recipes', cooking_recipes_path %> 
          <% if menu_active?(:recipes) %><span class="sr-only">(current)</span><% end %>
        </li>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Manage <span class="caret"></span></a>
          
          <ul class="dropdown-menu" role="menu">
            <li class="<%= menu_active?(:ingredients)? 'active' : nil %>">
              <%= link_to 'Ingredients', ingredients_path %>
              <% if menu_active?(:ingredients) %><span class="sr-only">(current)</span><% end %>
            </li>
            <li class="<%= menu_active?(:units)? 'active' : nil %>">
              <%= link_to 'Units', units_path %>
              <% if menu_active?(:units) %><span class="sr-only">(current)</span><% end %>
            </li>
          </ul>
        </li>
      </ul>
      <button type="button" class="btn btn-danger navbar-btn navbar-right">Register</button>

      <form class="navbar-form navbar-right" role="form">
        <div class="form-group">
          <input type="text" placeholder="Email" class="form-control">
        </div>
        <div class="form-group">
          <input type="password" placeholder="Password" class="form-control">
        </div>
        <button type="submit" class="btn btn-success">Sign in</button>
      </form>
      
    </div><!--/.navbar-collapse -->
  </div>
</nav>