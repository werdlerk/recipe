<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_path, class: 'navbar-brand' do %>
        <% if menu_active?(:brand) %>
          <%= fa_icon 'cutlery inverse border' %>
        <% else %>
          <%= fa_icon 'cutlery border' %>
        <% end %>
      <% end %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li class="<%= menu_active?(:recipes)? 'active' : nil %>">
          <%= link_to 'Recipes', cooking_recipes_path %> 
          <% if menu_active?(:recipes) %><span class="sr-only">(current)</span><% end %>
        </li>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Manage <span class="caret"></span></a>
          
          <ul class="dropdown-menu" role="menu">
            <li class="<%= menu_active?(:ingredients)? 'active' : nil %>">
              <%= link_to 'Ingredients', ingredients_path %>
              <% if menu_active?(:ingredients) %><span class="sr-only">(current)</span><% end %>
            </li>
            <li class="<%= menu_active?(:units)? 'active' : nil %>">
              <%= link_to 'Units', units_path %>
              <% if menu_active?(:units) %><span class="sr-only">(current)</span><% end %>
            </li>
          </ul>
        </li>
      </ul>
      <%= link_to 'Register', register_path, class:'btn btn-danger btn-sm navbar-btn navbar-right' %>

      <form class="navbar-form navbar-right" role="form">
        <div class="form-group">
          <div class="input-group">
            <span class="input-group-addon" id="email-field-addon">@</span>
            <%= email_field_tag 'email', '', class:'form-control input-sm', placeholder:'Email', 'aria-describedby'=> 'email-field-addon' %>
          </div>
        </div>
        <div class="form-group">
          <div class="input-group">
            <span class="input-group-addon glyphicon glyphicon-lock" id="password-field-addon"></span>
            <%= password_field_tag 'password', '', class:'form-control input-sm', 'aria-describedby' => 'password-field-addon' %>
          </div>
        </div>
        <button type="submit" class="btn btn-success btn-sm">Sign in</button>
      </form>
      
    </div><!--/.navbar-collapse -->
  </div>
</nav>